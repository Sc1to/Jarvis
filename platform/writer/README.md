# writer

Novel-writing app (Phase 10). FastAPI backend on port 8011; frontend in `frontend/writer/`.
Design spec: `docs/WRITER_SPEC.md`.

Tests: `pytest tests` from this directory (with the service venv active).
