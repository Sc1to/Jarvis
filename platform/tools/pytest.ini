[pytest]
testpaths = tests
# Adds platform/ dir to PYTHONPATH so 'import tools' resolves to this package,
# matching the production import path on the mini PC (/opt/platform/tools/).
pythonpath = ..
# asyncio_mode = auto  # enable when pytest-asyncio is installed
